import React from "react";
import {AppContextProvider} from "../AppContext/AppContext";
import CustomerContext, {CustomerContextProvider} from "../CustomerContext/CustomerContext";
import MenuItemsContext, {MenuItemsProvider} from "../MenuItemsContext/MenuItemsContext";
import OrdersContext, {OrdersContextProvider} from "../OrdersContext/OrdersContext";
import CartContext, {CartContextProvider} from "./CartContext/CartContext";

export default class ContextContainer extends React.Component{
    render(){
        return (
            <>

            </>
        );
    };
};